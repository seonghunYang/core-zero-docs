# Popover

팝오버는 트리거 요소를 기준으로 위치가 정해지는 오버레이 요소입니다. 이 오버레이에는 다양하고 자유로운 형태의 콘텐츠를 표시할 수 있습니다.

import { Popover } from "zeroless";
import Preview, { Item } from "/components/preview";
import { Tabs } from "nextra/components";

<Tabs items={["Preview", "Code"]}>
  <Tabs.Tab>
    <Preview>
      <Popover placement={"bottom"} offset={5}>
        <Popover.Trigger className="text-black/70 px-2 rounded-lg data-[active]:text-black data-[hover]:text-black data-[focus]:outline-1 data-[focus]:outline-primary">
          popover
        </Popover.Trigger>
        <Popover.Content className="shadow-lg text-sm/6 rounded-xl bg-white data-[closed]:animate-opacity-show-reverse data-[open]:animate-opacity-show data-[active]:ring-primary data-[active]:ring-2">
          <div className="p-3">
            <Item
              title="Insights"
              description="Measure actions your users take"
            />
            <Item
              title="Automations"
              description="Create your own targeted content"
            />
            <Item title="Reports" description="Keep track of your growth" />
            <Item
              title="Documentation"
              description="Start integrating products and tools"
            />
          </div>
        </Popover.Content>
      </Popover>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
```jsx
export default function Example() {
  return (
    <Popover placement={"bottom"} offset={10}>
      <Popover.Trigger className="text-black/70 px-2 rounded-lg data-[active]:text-black data-[hover]:text-black data-[focus]:outline-1 data-[focus]:outline-primary">
        popover
      </Popover.Trigger>
      <Popover.Content className="shadow-lg text-sm/6 rounded-xl bg-white data-[closed]:animate-opacity-show-reverse data-[open]:animate-opacity-show data-[active]:ring-primary data-[active]:ring-2">
        <div className="p-3">
          <Item
            title="Insights"
            description="Measure actions your users take"
          />
          <Item
            title="Automations"
            description="Create your own targeted content"
          />
          <Item title="Reports" description="Keep track of your growth" />
          <Item
            title="Documentation"
            description="Start integrating products and tools"
          />
        </div>
      </Popover.Content>
    </Popover>
  );
}

export const Item = function ({ title, description }) {
  return (
    <a
      className="block rounded-lg py-2 px-3 transition hover:bg-black/5"
      href="#"
    >
      <p className="font-semibold text-black">{title}</p>
      <p className="text-black/50">{description}</p>
    </a>
  );
};
```
  </Tabs.Tab>
</Tabs>

## Basic exmaple

```
code
```

### Controlled
